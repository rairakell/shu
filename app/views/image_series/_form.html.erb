<%= form_for(@image_series) do |f| %>
  <% if @image_series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_series.errors.count, "error") %> prohibited this image_series from being saved:</h2>

      <ul>
      <% @image_series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
