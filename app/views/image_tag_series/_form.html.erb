<%= form_for(@image_tag_series) do |f| %>
  <% if @image_tag_series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_tag_series.errors.count, "error") %> prohibited this image_tag_series from being saved:</h2>

      <ul>
      <% @image_tag_series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_tag_id %><br>
    <%= f.text_field :image_tag_id %>
  </div>
  <div class="field">
    <%= f.label :image_serie_id %><br>
    <%= f.text_field :image_serie_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
