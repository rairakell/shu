<%= form_for(@fiction) do |f| %>
  <% if @fiction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fiction.errors.count, "error") %> prohibited this fiction from being saved:</h2>

      <ul>
      <% @fiction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
